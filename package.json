{
  "dependencies": {
    "@openzeppelin/contracts": "^4.3.3",
    "@truffle/hdwallet-provider": "^1.5.1",
    "truffle-assertions": "^0.9.2"
  },
  "scripts": {
    "postinstall": "cd client && npm install && cd .. && cd oracle && npm install",
    "test": "echo blank"
  }
}
